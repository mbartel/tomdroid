<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.tomdroid"
    android:versionCode="15"
    android:versionName="0.8.0dev" >

    <uses-sdk android:minSdkVersion="4" />

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.android.launcher.permission.INSTALL_SHORTCUT" />
    <uses-permission android:name="com.android.launcher.permission.UNINSTALL_SHORTCUT" />

    <instrumentation
        android:name="android.test.InstrumentationTestRunner"
        android:targetPackage="org.tomdroid" />

    <application
        android:debuggable="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:testOnly="true"
        android:theme="@android:style/Theme.Light.NoTitleBar" >
        <activity
            android:name="org.tomdroid.ui.Tomdroid"
            android:configChanges="orientation|screenSize"
            android:label="@string/app_name"
            android:theme="@style/LightTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="sync"
                    android:scheme="tomdroid" />
            </intent-filter>
            <!-- enable the search dialog to send searches to SearchableActivity -->
            <meta-data
                android:name="android.app.default_searchable"
                android:value=".ui.Tomdroid" />

            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity
            android:name="org.tomdroid.ui.ViewNote"
            android:label="@string/view_title"
            android:theme="@style/LightTheme" >
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:mimeType="vnd.android.cursor.item/vnd.tomdroid.note" />
            </intent-filter>
        </activity>
        <activity
            android:name="org.tomdroid.ui.EditNote"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:label="@string/edit_title"
            android:theme="@style/LightTheme" >
        </activity>
        <activity
            android:name="org.tomdroid.util.Receive"
            android:theme="@style/LightTheme" >

            <!-- To receive data from other apps via share -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>
            <!-- To receive note files from file browser (open all .note files) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="file" />
                <data android:host="*" />
                <data android:port="*" />
                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\\.note" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="content" />
                <data android:host="*" />
                <data android:port="*" />
                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\\.note" />
            </intent-filter>
            <!-- To open all text files (open all text files) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="content" />
                <data android:host="*" />
                <data android:port="*" />
                <data android:pathPattern=".*" />
                <data android:mimeType="text/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="file" />
                <data android:host="*" />
                <data android:port="*" />
                <data android:pathPattern=".*" />
                <data android:mimeType="text/*" />
            </intent-filter>
            <!-- To receive note files from mail client (open with application/note mimetype) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="file" />
                <data android:host="*" />
                <data android:port="*" />
                <data android:mimeType="application/note" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="content" />
                <data android:host="*" />
                <data android:port="*" />
                <data android:mimeType="application/note" />
            </intent-filter>
        </activity>

        <provider
            android:name="org.tomdroid.util.SearchSuggestionProvider"
            android:authorities="org.tomdroid.util.SearchSuggestionProvider" />
        <provider
            android:name="org.tomdroid.NoteProvider"
            android:authorities="org.tomdroid.notes" />

        <activity
            android:name="org.tomdroid.ui.PreferencesActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/pref_title"
            android:theme="@style/LightTheme" >
        </activity>
        <activity
            android:name="org.tomdroid.ui.ShortcutActivity"
            android:label="@string/shortcut_menu_item_caption"
            android:theme="@style/LightTheme" >
            <intent-filter>
                <action android:name="android.intent.action.CREATE_SHORTCUT" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name="org.tomdroid.ui.CompareNotes"
            android:label="@string/app_name"
            android:theme="@style/LightTheme" >
        </activity>
        <activity
            android:name="org.tomdroid.ui.FilePickerActivity"
            android:label="@string/filePickerTitle"
            android:theme="@style/LightTheme" />

        <uses-library android:name="android.test.runner" />

        <provider
            android:name="com.android.tools.ir.server.InstantRunContentProvider"
            android:authorities="org.tomdroid.com.android.tools.ir.server.InstantRunContentProvider"
            android:multiprocess="true" />
    </application>

</manifest>